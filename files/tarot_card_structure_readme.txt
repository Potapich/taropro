
📘 README: Структура JSON-объекта карты Таро

---

🔹 Общая структура:
Каждая карта представлена объектом внутри массива `cards`, сгруппированного по языку (`languages → ru`).

---

🔸 Основные поля карты:

- `id` (string) — Уникальный идентификатор карты (например, "0" для Дурака).
- `card_order` (string) — Порядковый номер карты в колоде для сортировки.
- `major_card` (boolean) — Флаг: true, если карта относится к старшим арканам.
- `suit` (null|string) — Масть для младших арканов ("wands", "cups", "swords", "pentacles") или null для старших.
- `image` (string) — Имя файла изображения карты (например: "major_arcana_fool.webp").

---

🔸 Символические и астрологические данные:

- `astrology` (object):
  - `planet` (string) — Планета, ассоциированная с картой (если есть).
  - `zodiac` (string) — Знак зодиака, связанный с картой (если применимо).

- `numerology` (string) — Нумерологическое значение карты.

---

🔸 Атмосферные поля (для UX/мистической подачи):

- `mantra` (string) — Краткое утверждение/аффирмация, связанная с энергией карты.
- `metaphor` (string) — Метафорическое описание образа карты.
- `title` (string) — Название карты.

- `keywords` (array of strings) — Ключевые слова, описывающие карту. Используются для SEO, фильтрации, поиска.

---

🔸 Основные текстовые поля:

- `main_description` (string) — Краткое описание карты (≥ 45 слов).
- `full_description` (string) — Расширенное толкование карты (≥ 80 слов).
- `symbolism` (string) — Объяснение символики изображения карты.
- `magic_description` (string) — Применение карты в магических/ритуальных практиках.
- `day_description` (string) — Значение карты, если она выпадает как карта дня.
- `summary_description` (string) — Краткое резюме карты (1–2 предложения).
- `advice_upright` (string) — Практический совет, если карта выпала прямо.
- `advice_reversed` (string) — Практический совет, если карта выпала перевёрнуто.

---

🔸 Энергетические и тематические поля:

- `emotion` (string) — Основной эмоциональный оттенок карты (например: "надежда", "вдохновение", "страх").
- `element` (string) — Стихия: "fire", "water", "air", "earth" (чаще применяется к младшим арканам).
- `energy` (string) — Энергетика карты: "active", "passive", "neutral".

---

🔸 Аспекты (aspects):

Массив тем, по которым можно интерпретировать карту (в раскладе или вопросе).

Каждый объект содержит:
- `aspect` (string) — Техническое имя аспекта (например: "love").
- `label` (string) — Название аспекта для вывода в интерфейсе.
- `description` (string) — Пояснение, на какие темы аспект влияет.
- `upright` (array of strings) — Интерпретации карты в прямом положении по данному аспекту.
- `reversed` (array of strings) — Интерпретации карты в перевёрнутом положении по аспекту.

Рекомендуемые аспекты: love, work, finance, health, spirit, self, family, friends, decision, future, lesson, creativity, travel, karma

---

✅ Структура поддерживает уровни 1.5 → 2.0 → 3.0 интерпретации карт.
